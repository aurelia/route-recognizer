import { RouteRecognizer } from '../src/index';

describe('route recognizer', () => {
  it('should have some tests', () => {
    var recognizer = new RouteRecognizer();
    expect(recognizer).not.toBe(null);
  });
});